# Umsetzungsprotokoll mod_seminarplaner

**Was hier hineingehört:** Was tatsächlich gebaut wurde — Datum, Plugin-Version, betroffene
Dateien, wie es verifiziert wurde. Geschrieben aus den Code-Sessions heraus.

**Was hier NICHT hineingehört:** Richtungsentscheidungen (D1–D65). Die stehen ausschließlich im
Konzeptdokument `docs/seminarplaner-umbau-konzept.md` im Projektordner (außerhalb dieses Repos),
das im Claude-Projekt gepflegt wird. Dieses Repo hielt davon lange eine zweite Kopie — beide
Fassungen wurden aus verschiedenen Richtungen fortgeschrieben und liefen inhaltlich auseinander
(gleiche Historien-Buchstaben für verschiedene Einträge, dieselbe Nummer D63 für zwei
verschiedene Entscheidungen). Die Kopie ist deshalb aufgelöst; Entscheidungen und
Umsetzungsprotokoll sind jetzt getrennt, damit die Divergenz nicht wiederkehren kann.

Ältere Umsetzungen (vor Juli 2026) sind nicht rückwirkend erfasst — sie stehen in der
Git-Historie.

---

## Protokoll (jüngste zuerst)

- 15. Juli 2026: **Split-Teile zusammenführen + Picker-Filter „nur unbenutzte" + gleich große Vorschlagskarten** (Claude Code, Version 2026071550, Auftraggeber-Befunde, browser-/node-verifiziert). **(1) Merge benachbarter Split-Teile:** Zwei direkt aufeinanderfolgende Platzierungen derselben `splitgroup` im selben Anker (z. B. nachdem eine bereits fortgesetzte Einheit durch eine Pause erneut geteilt wurde und die Teile benachbart landen) werden zu einer Platzierung verschmolzen (Dauern summiert); bleibt nur ein Teil der Gruppe, fällt die Split-Markierung weg. Neuer Helfer `mergeAdjacentSplitParts()`, aufgerufen am Anfang von `render()` (heilt auch bestehende Pläne, setzt dann dirty). Über die Mittagspause getrennte Teile liegen in verschiedenen Ankern und bleiben getrennt. Node-Harness: 15+15→30 verschmilzt, 65/25 über Mittag bleibt getrennt. **(2) Picker-Filter:** Häkchen „Nur noch nicht verwendete Einheiten anzeigen" unter dem Suchfeld (`#sq-picker-unused`, State `pickerUnusedOnly`, sitzungsweit gemerkt) blendet in `renderPickerList` alle bereits platzierten Karten aus; das Badge „bereits verwendet" bleibt (doppelt genäht). Verifiziert cmid 124: 19→7 Zeilen, 12 verwendete raus. **(3) Vorschlagskarten gleich groß:** `.sq-suggest__card` jetzt feste Breite 240px + feste Höhe 150px (`flex: 0 0 240px`, `box-sizing`), Titel/Begründung auf 2 Zeilen begrenzt (line-clamp), Button unten (`margin-top:auto`); vorher variierten neu hinzugekommene (globale/„bereits im Plan") in Breite/Höhe. Verifiziert: alle Karten 240×150, Buttons nicht abgeschnitten.

- 15. Juli 2026: **Baustein-Alternativen auch über die Mittagspause (Mehr-Anker-Guard entfernt, sicher gemacht)** (Claude Code, Version 2026071547, Auftraggeber-Wunsch, browser-verifiziert). Bisher blockierte ein Guard (Version 2026071538) Alternativen für Bausteine mit mehr als einem Lauf (D3-Fortsetzung über die Mittagspause), weil `chooseVariant`/`linkAlternative` nur den ersten Lauf (`locateBausteinRun`) fassten → Fortsetzung ginge beim Tausch verloren. Jetzt Mehr-Anker-sicher: neuer Helfer `collectAndRemoveRuns(bid)` nimmt **alle** Läufe aus der Zeitleiste (pids in Reihenfolge, Einfügestelle des ersten Laufs; Entfernen von hinten für gültige Indizes). `chooseVariant`/`ensureVariant`/`linkAlternative` nutzen ihn statt `locateBausteinRun`; die Warnung in `renderBausteinAltField` und der `isSingleRunBaustein`-Filter in `alternativeCandidateBausteine` sind entfernt (alle Plan-Bausteine sind Kandidaten). **Verhalten (Auftraggeber-Entscheidung):** eine über die Mittagspause laufende Alternative kommt als **ein zusammenhängender Block** an der Stelle des ersten Laufs zurück; ist sie zu lang, greift die normale Überlauf-Aktion (kein Auto-Split). Kein Datenverlust: auf moodle501umbau cmid 123 Dienstag am echten Mehr-Anker-Baustein „PRAXIS" verifiziert — Warnung weg, 6 Kandidaten im Dropdown; Alternative verknüpft → getauscht → zurückgetauscht → **reload**: alle 5 PRAXIS-Einheiten (inkl. 30/15-Split-Teile) erhalten, keine Dubletten, geparkte Alternative korrekt; PRAXIS kam als 180-Min-Block zurück (passte in den VM). Testartefakt danach wieder gelöst. `isSingleRunBaustein` bleibt (ungenutzt) definiert.

- 15. Juli 2026: **Überblick: Mittagspause sichtbar, Einheit hat Vorrang** (Claude Code, Version 2026071546, Auftraggeber-Wunsch, browser-verifiziert). Die Wochen-Projektion (`overviewPlanDays` in `grid.js`) zeigte die Mittagspause bisher nicht (nur eine Lücke zwischen VM- und NM-Blöcken). Jetzt wird pro Tag ein `break`-Item „Mittagspause" eingefügt: **Start = max(konfiguriertes VM-Ende, tatsächliches Ende der VM-Inhalte)**, **Ende = NM-Start**. Damit hat die Seminareinheit/der Baustein **Vorrang** — läuft der Vormittag in die Pause hinein, beginnt die Pause erst am Einheiten-Ende (verkürzt sich), statt überlagert zu werden; reicht die VM-Belegung bis zum NM-Start, entfällt die Pause. Nur Anzeige (Überblick ist read-only, Sequenz unverändert). Verifiziert cmid 124 Plan #25: 5 Mittagspausen-Blöcke Mo–Fr mit Zeiten, an einem Tag „12:20–14:00" (VM lief bis 12:20 → Pause verkürzt), keine Konsolenfehler.

- 15. Juli 2026: **Picker mit Quellen-Tabs + Ausgrauen + Empfehlungen verfeinert** (Claude Code, Version 2026071545, Auftraggeber-Wunsch, browser-verifiziert). **(1) Tabs im „Einheit hinzufügen"-Modal** (`openPicker`/`renderPickerList`): **Lokale Bibliothek** · **Methodensammlung** (globale Sets aus `this.globalMethods`/`browse_global_library`, Übernehmen läuft über `adoptAndPlace` = lokale Kopie D33) · **Importiertes Konzept** (nur wenn vorhanden — Karten aus einem Seminarkonzept-Import tragen ID-Präfix `konzept-`; der lokale Tab blendet diese aus). Neuer State `pickerTab`, Handler `picker-tab`; `picker-add` verzweigt bei `data-global-methodid` auf `adoptAndPlace`, sonst `applySuggestTarget`. **(2) Schon verwendete Einheiten ausgegraut** in Picker (`sq-picker__row--used` + Badge „bereits verwendet") und Empfehlungen (`sq-suggest__card--used`) — weiter wählbar (Opacity, Hover voll); Erkennung über neue `placedCardRefs()` (aktive Karten-IDs aller Platzierungen). **(3) Empfehlungsalgorithmus** (`suggestFor`): max **6** statt 4; schon verwendete werden **nach unten sortiert** (bleiben wählbar); Themen-Treffer (Unterthemen/Themenplan, neue `topicKeywords`) zählen **zusätzlich ×3** zu den allgemeinen Stichwort-Treffern ×2 (Kontext stärker); globale Treffer sind score-gleichwertig (keine Quellen-Abwertung). Auf moodle501umbau cmid 124 verifiziert: Tabs (Lokal 16 / Methodensammlung 60 mit Set-Name + Global-Button, kein Konzept-Tab da nichts importiert), verwendete grau mit Badge, Empfehlungen exakt 6 mit verwendeten nach unten, keine Konsolenfehler. **Noch nicht live geprüft:** dritter Tab (kein Seminarkonzept in cmid 124 importiert) und der Global-Übernehmen-Klick aus dem Picker (Routing auf das bewährte `adoptAndPlace`).

- 15. Juli 2026: **Split-Regel gelockert: feste Mindestgröße 15 Min. statt ⅓ der Einheit** (Claude Code, Version 2026071544, Auftraggeber-Befund cmid 124, browser-verifiziert). Die ursprüngliche Regel (`canSplit`, beide Teile ≥ `max(15, ⌈dauer/3⌉)`) verhinderte gewünschte Splits: eine 90-Min-Einheit mit 25 Min Überhang bräuchte beide Teile ≥ 30 Min, die 25-Min-Fortsetzung war zu klein → ganze Einheit wanderte. Auftraggeber will hier den 65/25-Split. Neue Regel: nur noch feste **Mindestgröße 15 Min. je Teil** (kein Anteil an der Gesamtlänge mehr), `minpiece = 15`. Damit teilt sich 90→65/25 und 60→30/30, aber ein 10-Min-Stub vor der Pause wird weiter verhindert (ganze Einheit wandert). Node-Regeltest + auf moodle501umbau cmid 124 verifiziert: Montag-Vormittag 90-Min-Einheit → 65 Min bleiben (225/225), 25 Min Fortsetzung am Nachmittag, gespeichert, keine Konsolenfehler.

- 15. Juli 2026: **Leeren Baustein in der Sequenz anlegen** (Claude Code, Version 2026071543, Auftraggeber-Wunsch, browser-verifiziert). Bisher entstand ein Baustein nur, indem man einer bestehenden Einheit eine Überschrift gab; einen leeren Baustein (nur Titel) konnte man nur aus der Migration bekommen. Neu: Button „**＋ Baustein**" je Anker, **links neben „＋ Einheit hinzufügen"** (`add-baustein`). `createEmptyBaustein(anker)` legt einen Baustein („Neuer Baustein") + einen ungefüllten Platzhalter (leere `einheitenauswahl` `{kandidaten:[],aktiv:null}`, `dauer:0`, wie ein migrierter Reservierungs-Platzhalter D1) ans Anker-Ende — ohne reservierte Dauer (Auftraggeber-Vorgabe). Rendering: der leere Baustein zeigt **kein** „0 Min. reserviert"-Badge (Guard in `renderSequence`) und statt leeren Inhalts eine Einladung „Noch keine Einheit … ＋ Einheit hinzufügen" (`renderBausteinContent`, Zweig `gapminutes<=0`). Befüllen: der Button (`baustein-add-unit`) öffnet den Picker **zielgerichtet auf den Baustein** — dafür führt `openPicker(anker, targetPid)` jetzt ein `pickerTarget` (Anker **oder** Baustein-Platzhalter), `picker-add`/`picker-create` routen über `applySuggestTarget`; `addCardToBaustein` verbraucht den 0-Min-Platzhalter beim Platzieren automatisch (rest ≤ 0 → Platzhalter + leere Auswahl gelöscht). CSS `.sq-baustein__empty`/`__emptyhint`. Auf moodle501umbau verifiziert: Button-Reihenfolge (Baustein·Einheit·Pause), Anlegen (Titel „Neuer Baustein", kein Reserviert-Badge), Picker-Ziel = Baustein, Platzieren → Einheit im Baustein (60 Min., Platzhalter weg), Persistenz reload-fest, keine Konsolenfehler. Titel ändert man über „Bearbeiten" (bestehender Baustein-Editor). **Offen (bewusst klein gehalten):** Löscht man alle Einheiten eines so angelegten Bausteins, bleibt ein unsichtbarer Baustein-Eintrag ohne Platzierung in `sequenz.bausteine` (harmlos, rendert nie); Titel-Abfrage direkt beim Anlegen (statt Default + Bearbeiten) wäre optional.

- 15. Juli 2026: **Zeilen-Menü (⋮) wurde am Anker-Rand abgeschnitten** (Claude Code, Version 2026071542, Nutzer-Befund). Das nach unten aufklappende `.sq-menu__panel` wurde vom `overflow: hidden` des `.sq-anchor` beschnitten — bei Zeilen nahe der Anker-Unterkante fehlte der letzte Eintrag („Aus dem Plan entfernen"), sichtbar v. a. beim 5-Punkte-Menü freistehender Einheiten (Nach vorne/hinten, In „x" aufnehmen, Überschrift geben, Entfernen). Das `overflow: hidden` klippt nur den Anker-Kopf an die (seit CD-Stufe 1 eckigen, `--sq-radius: 0`) Ecken. Fix in `styles.css`: `.sq-anchor:has(.sq-menu__panel.open) { overflow: visible; }` — hebt die Beschneidung gezielt nur auf, solange ein Menü offen ist (Ecken-Clipping bleibt sonst erhalten; einziger clippender Vorfahr ist der Anker, sq-baustein/sq-unit clippen nicht).

- 15. Juli 2026: **Einheiten-Split beim Tagesüberlauf (statt nur Ganz-Verschieben)** (Claude Code, Version 2026071541, auf Staging deployt+browser-verifiziert; auf Auftraggeber-Wunsch trotz Testlauf umgesetzt). Bisher schob die geführte Überlauf-Aktion (`resolveOverflow`, C1) immer *ganze* Platzierungen in den nächsten Abschnitt. Neu: eine gefüllte Einheit, die genau über die Anker-Grenze läuft, wird **geteilt** — der Teil, der noch passt, bleibt; der Rest läuft im nächsten Abschnitt/Tag als **Fortsetzung** weiter. **Split-Regel (didaktische Festlegung des Auftraggebers):** nur teilen, wenn *beide* Teile ≥ ⅓ der Einheit **und** ≥ 15 Min. sind (`canSplit`: `minpiece = max(15, ⌈dauer/3⌉)`); sonst wandert die Einheit ganz (kein 10-Min-Stub vor Pause/Tagesende). `resolveOverflow` iteriert jetzt: ganze Platzierung verschieben, solange die letzte über die Grenze hinausragt, und die grenzüberschreitende Einheit splitten, sobald `canSplit` greift (danach passt der Anker exakt). Neue Methoden in `amd/src/sequenz.js`: `canSplit`, `splitPlacement` (setzt `origin.dauer` = bleibender Teil, legt Fortsetzungs-Platzierung mit eigener kopierter `einheitenauswahl` an, beide Teile tragen denselben `splitgroup`-Marker, die Fortsetzung zusätzlich `fortsetzung:true`), `cleanupSplitGroup` (räumt den Marker beim Löschen des einen Teils auf). Alternative-Swap (`renderSwap`) ist für Split-Teile gesperrt (wie bei Baustein-Fortsetzungen), damit die manuell gehaltenen Dauern nicht aus der Karte übersynchronisiert werden; `renderPlacement` zeigt bei freistehenden Einheiten ein „Fortsetzung"-Badge (im Baustein trägt es der Kopf über dieselbe `bausteinid`). Überlauf-Hinweistext angepasst. Neue Felder `splitgroup`/`fortsetzung` überleben den PHP-Speicher-Roundtrip (keine Feld-Whitelist in `sequence_state`). Split-Regel per Node gegen die Auftraggeber-Beispiele geprüft (60→30/30 teilt, 10-Min-Reste wandern ganz); auf moodle501umbau browser-verifiziert: 60-Min-Einheit an der Grenze → 25/35-Split, Vormittag exakt gefüllt (225/225), Fortsetzung am Nachmittag, gespeichert und reload-fest, keine Konsolenfehler. **Offen:** Die zugrunde liegende Richtungsentscheidung (Einheiten-Splitting überhaupt zulassen, Schwellenregel) gehört noch in das kanonische Konzeptdokument im Claude-Projekt (dieses Repo führt keine Entscheidungen).

- 15. Juli 2026: **Fix Baustein-⇄-Popover: abgeschnitten + blockierte Einheiten-Klicks** (Claude Code, Version 2026071540, auf Staging deployt+browser-verifiziert). Auftraggeber-Befund: Popover des Baustein-⇄-Chips wurde abgeschnitten angezeigt UND „keine Einheiten mehr zu Bausteinen machen". Ursache (eine): `.sq-swap__panel` ist `position:absolute; right:0` — passt beim Einheiten-Chip (rechts in der Zeile), aber der Baustein-Chip sitzt LINKS im Kopf, dadurch lief das Popover mit langem Titel über den linken Rand (abgeschnitten) und überlagerte als absolut positioniertes Element die darunterliegenden Einheiten → fing deren Klicks ab (⋮-Menü/„Überschrift geben" nicht erreichbar). Fix: `renderBausteinSwap` bekommt Modifier `sq-swap--baustein`; CSS `.sq-swap--baustein .sq-swap__panel { left:0; right:auto; max-width:min(420px,72vw); z-index:30 }` + Umbruch langer Titel (`white-space:normal`, `align-items:flex-start`). Browser-verifiziert: Popover öffnet linksbündig, langer Titel bricht um, nichts abgeschnitten; „Überschrift geben" per ECHTER Tastatur getestet (getippter Wert kommt an, Feld behält Fokus, Baustein wird angelegt) — der Heading-Flow selbst war nie defekt, nur vom überlagernden Popover blockiert. Testartefakte danach entfernt.

- 15. Juli 2026: **Baustein-Alternativen an das Seminareinheiten-Bedienkonzept angeglichen** (Claude Code, Version 2026071539, auf Auftraggeber-Wunsch: „kein neues Bedienkonzept"). Weg vom eigenen Header-Button + Picker-Modal + Pillen, hin zum vorhandenen Einheiten-Muster: **(1)** Auswahl der Alternativen wandert in den **Baustein-Bearbeiten-Modal** — Multi-Dropdown „Alternative Bausteine" **unter Unterthemen** (dieselbe `kg-tag-dropdown`-Komponente wie „Alternative Seminareinheiten"). Optionen: bereits verknüpfte Alternativen (Wert `v:<vid>`, vorausgewählt) + geeignete andere Plan-Bausteine (`b:<bid>`). `saveBausteinEditor` gleicht ab: abgewählte Varianten → `unlinkAlternative`, neu gewählte Bausteine → `linkAlternative`. **(2)** Am Baustein-Kopf erscheint — nur wenn Alternativen existieren — der **„⇄ Alternative"-Chip** (neue `renderBausteinSwap`, exakt wie `renderSwap` bei Einheiten) mit Popover zur Auswahl der aktiven Variante; Pillen und Header-Button entfielen. `bindMultiDropdown` aus `bindUnitMultiDropdowns` extrahiert und im Baustein-Modal wiederverwendet; neuer State `openBausteinSwapBid` (+ Outside-Click-Handling); `linkAlternative`/`unlinkAlternative` geben jetzt boolean zurück und rendern/toasten nicht mehr (gebündelt im Save). `openAlternativePicker` + Pillen-CSS (`sq-pill__x`/`sq-altpick`) entfernt. Titel-Feld im Modal editiert die aktive Variante. Node-Harness weiter grün (Verknüpfen/Umschalten/Lösen/Mehr-Anker-Guard). Auf Staging browser-verifiziert.

- 15. Juli 2026: **Baustein-Alternativen: Guard gegen Mehr-Anker-Bausteine** (Claude Code, Version 2026071538, auf Staging deployt+verifiziert). Beim Browser-Test auf moodle501umbau (cmid 83) fiel auf: Baustein „2. Systematik…" läuft über die Mittagspause (D3-Fortsetzung = zwei Anker-Läufe). `linkAlternative`/`chooseVariant` fassen aber nur EINEN Lauf (`locateBausteinRun` = erster Treffer) → ein solcher Baustein hätte beim Parken seine Fortsetzung verloren (Korruption). Neue Helfer `bausteinRuns(bid)` (alle Läufe über alle Anker) und `isSingleRunBaustein(bid)`; `openAlternativePicker` zeigt für einen Mehr-Anker-Host einen Erklärungs-Hinweis statt der Liste und filtert Mehr-Anker-Kandidaten aus; `linkAlternative` lehnt Host oder Ziel mit >1 Lauf per Toast ab. Damit ist die Ein-Block-Annahme der Tauschmechanik durchgesetzt (volle Mehr-Anker-Unterstützung bewusst zurückgestellt). Node-Harness um den Guard-Fall erweitert, grün. Auf Staging browser-verifiziert (Button erscheint in allen Baustein-Köpfen, Picker listet die geeigneten Bausteine korrekt mit Einheiten/Dauer).

- 15. Juli 2026: **ZIM-PDF im Querformat + Baustein-Alternativen (D8)** (Claude Code, Version 2026071537, noch NICHT deployt/browser-verifiziert). **(1) ZIM-PDF Querformat:** `exportPdfZim` in `amd/src/importexport.js` erzeugt das Dokument jetzt mit `new jsPDF({orientation: 'landscape'})` — mehr Platz für die nebeneinanderstehenden ZIM-Spalten. Nur der ZIM-Export ist betroffen; Konzeptsammlung und Materialliste bleiben Hochformat. Die Spaltenbreiten passen sich ohnehin dynamisch über `pageSize.getWidth()` an; im geteilten `drawPdfTitlePage` wurde nur die Deckblatt-Trennlinie von fest `196` auf `getWidth() - 14` umgestellt (in Hochformat unverändert, im Querformat zieht sie mit). **(2) Baustein-Alternativen:** Ein Baustein kann jetzt einen anderen Baustein desselben Plans als Alternative aufnehmen (Konzept: C2/D8, „Alternative dazu-wählen"). Neuer „⇄ Alternative"-Button im Baustein-Kopf öffnet einen Picker der übrigen Plan-Bausteine; der gewählte wird aus der Zeitleiste geparkt und als Variante geführt, Titel-Pillen schalten den ganzen Block (inkl. Einheiten) um, ein ✕ an der geparkten Pille löst die Alternative wieder als eigenständigen Baustein. Reuse der bestehenden `varianten`/`aktivevariante`-Struktur; `chooseVariant` sichert vor dem Tausch den Live-Stand in die abgehende Variante zurück (kein Verlust von Edits an der aktiven Variante). Neue Methoden in `amd/src/sequenz.js`: `locateBausteinRun`, `bausteinTitle`, `ensureVariant`, `linkAlternative`, `unlinkAlternative`, `openAlternativePicker`; `normalizeSequenz` normalisiert Varianten-Form + `aktivevariante`-Fallback (übersteht den {}→[]-Roundtrip); Baustein-Kopf zeigt den Titel der aktiven Variante. CSS in `styles.css` (Pillen-✕, Alternative-Picker-Zeilen). Kernlogik (Verknüpfen → Umschalten inkl. Sync → Lösen mit Collapse) per Node-Harness isoliert getestet — **Browser-Sichtprüfung auf Staging + PHP-Speicher-Roundtrip der geparkten Platzierungen stehen noch aus.**

- 15. Juli 2026: **Lernziel-Editor in gemeinsames Modul ausgelagert + dritter Einsatzort (Bibliothek)** (Claude Code, Version 2026071536). Der Differenzierer lag als Klassen-Methode in `sequenz.js`; damit ihn auch die Bibliothek nutzen kann, ist er jetzt ein eigenständiges AMD-Modul `amd/src/lernzieleditor.js` (Export `open(onAccept)` / `close()`, onAccept bekommt Satz + abgeleitete Seminarphase). `sequenz.js` verliert die DIFF-Daten und die `openLernzielEditor`/`closeLernzielEditor`-Methoden und ruft `LernzielEditor.open(...)` an beiden bisherigen Stellen; `suggestUnitPhase` bleibt. **Neu:** Am Lernziele-Feld des Methoden-Editors in `methodlibrary.php` sitzt jetzt derselbe Button „✎ Lernziel formulieren" (`#ml-lz-open-lernziele`); `methodlibrary.js` hängt den erzeugten Satz per `getFieldValue`/`setFieldValue` (Tiny-fähig) ans `#ml-e-lernziele` und belegt über `suggestEditorPhase` die Seminarphase (`#ml-e-seminarphase`) vor. Kein Verhaltensbruch: Satzlogik/Phasen-Mapping unverändert (nur verschoben), per `define`-Shim in Node geladen und getestet. Neue Build-Kopie `amd/build/lernzieleditor.min.js`. **Browser-Sichtprüfung auf Staging steht noch aus.**

- 15. Juli 2026: **Lernziel-Editor durch „Der Differenzierer" ersetzt** (Claude Code, Version 2026071534). Der bisherige D62-Baukasten (Phase → Verb → Inhalt → Satz „Die Teilnehmenden können …") ist durch den vom Auftraggeber gebauten Lernziel-Generator „Der Differenzierer" abgelöst (Vorbild: „The Differentiator" von Ian Byrd). Fünf Spalten mit gruppierten Chips und Würfeln (Denkoperation nach Bloom, Inhalt, Quelle, Produkt, Arbeitsform), optionales Thema-Feld, farbige Satzbausteine, „Ich kann …"-Form, fünf Satzmuster mit „Andere Formulierung", „Alles würfeln", „Zurücksetzen"; der Satz lässt weg, was leer bleibt. Ein Editor, weiterhin zwei Einsatzorte: Button „✎ Lernziel formulieren" am Lernziele-Feld des Einheiten-Modals (D21) und „✎ Formulieren" an den Seminarzielen (D61). **Phasen-Vorbelegung (D41) erhalten:** da der Generator nach Bloom gruppiert, mappt `DIFF_PHASE_BY_GROUP` die Bloom-Gruppe der gewählten Denkoperation auf eine Seminarphase (Kennen→Orientierung, Verstehen/Analysieren→Analyse, Anwenden/Erarbeiten→Handlungsteil, Bewerten→Transfer) — im Einheiten-Modal wird die Seminarphase daraus wie bisher vorbelegt. Dateien: `amd/src/sequenz.js` (+ Build-Kopie), `styles.css` (breiterer Modal, Spalten-Raster, Kategoriefarben). Entfernt: alte `LERNZIEL_VERBS`/`buildLernzielSentence`/`currentUnitPhaseKey`. Satzbau- und Mapping-Logik per Node isoliert getestet (Teilbelegung, Musterauswahl, Phasenableitung); **Browser-Sichtprüfung auf Staging steht noch aus.**

- 14. Juli 2026: **Handoff-Abgleich der Ansichten + Handout-Berechtigungen** (Claude Code, Versionen 2026071522–2026071529).
  **Roter Faden** (2026071522) nach `docs/design_handoff_seminarplaner/ROTER-FADEN.md` neu gebaut: durchgehender roter Faden, quadratische Meilenstein-Knoten, Tageskarten mit Dreieck, Anker Vormittag/Nachmittag, phasengetönte Blöcke mit Uhrzeit-Pille, Dauer und aufklappbarer Themenliste, Stil-Umschalter Modern/Kompakt. Dafür trägt der veröffentlichte Snapshot jetzt zusätzlich den `sequenz`-Abschnitt und einen kompakten Karten-Auszug (`methodcards`: aktive Einheiten mit Titel + Seminarphase) — Bestandspläne müssen einmal neu veröffentlicht werden, sonst greift der Legacy-Fallback ohne Phasen.
  **Handout-PDF** (2026071523): erzeugen Teilnehmende jetzt selbst im Roter-Faden-Tab (jsPDF auf der Seite); vorher war der Button ein Deep-Link auf den Import/Export-Tab und deshalb hinter `managegrids` versteckt — das „Handout für Teilnehmende" war für Teilnehmende unerreichbar. Generator liegt im gemeinsamen Modul `handout.js`, die Ableitung in `roterfadenmodel.js` (Bildschirm und PDF rechnen aus demselben Modell). **Sicherheitslücke geschlossen:** `importexport.php` verlangte nur `:view` und war damit per URL auch für Teilnehmende erreichbar; jetzt eine der vier Manage-Capabilities.
  **Sequenz** (2026071524/25): Zeilen aufgeräumt — sichtbar bleiben „⇄ Alternative" und „Bearbeiten", alle Nebenaktionen (Reihenfolge, Zugehörigkeit, Überschrift geben, Entfernen) liegen in einem ⋮-Kontextmenü mit Icons. Zeile trägt zusätzlich die Gruppengröße als Badge.
  **D35-Übersetzungsanzeige** (2026071526): zeigt links die echte Überblick-Optik und rechts die echten Sequenz-Bausteine statt zweier vereinfachter Skizzen. Nebenbefund gefixt: die `--sq-*`-Token lagen nur auf `.sq-shell`, das Modal hängt an `document.body` → Budget-Balken blieb farblos; Selektor jetzt `.sq-shell, .sq-modal-overlay`.
  **Überblick** (2026071527): tiefrote Hinweisleiste mit Primär-Button; die zwei Kästen und die PDF-Leiste zu einer schmalen Werkzeugleiste zusammengezogen (Plan-Auswahl + „Nur Ansicht"-Badge, zweite Zeile mit Veröffentlichen + PDF-Export). Tagesspalten behalten bewusst die Tagesdauer (kein Datum — Datenmodell kennt keine Kalenderdaten).
  **Bibliothek** (2026071528): Emoji-Badges raus, Phasen-Badge in Phasenfarbe getönt. **CSS-Falle dokumentiert:** der Abschnitt „Badges neutral" setzt `.sp-badge {background/border/color !important}` — ältere Tönungsregeln (`.ml-phase--* .sp-badge--phase`) waren deshalb toter Code.
  **Fix** (2026071529): `project_sequence_to_days()` setzte bei mehrteiligen Bausteinen `endMin` nur auf die Dauer der ersten Platzierung; der Baustein-Block im veröffentlichten Snapshot war damit zu kurz. Test `test_publish_projects_baustein_with_full_duration` deckt es ab.
  Der Einreichen-Tab bleibt auf Auftraggeber-Wunsch unverändert; der im Handoff vorgesehene Button „+ Zur Sequenz" auf Bibliothekskarten wird bewusst nicht gebaut (alles ist ohnehin im Sequenz-Picker wählbar).

- 13. Juli 2026 (bk): **Aufräumpunkt erledigt** (Claude Code, Versionen 2026071509/2026071510): `planningmode.php` stillgelegt (jetzt Redirect auf die Sequenz, Muster wie `methods.php` → Bibliothek) – kompatibilitätssicher gewählt, weil der Überblick (`grid.js`) noch dorthin verlinkt; ein Löschen hätte einen 404 erzeugt. Zusätzlich `role="status"`/`aria-live="polite"` an allen `kg-status`-Statuszeilen (Überblick, Import/Export, Roter Faden, Bibliothek, Einreichen). **Wichtig:** Die Update-Migration alt→neu bleibt unberührt (sie läuft über die Upgrade-Schritte und den Grid→Sequenz-Konverter, nicht über diese Seiten) – auf Staging verifiziert: alter migrierter Plan lädt und ist weiter nutzbar. Die nun toten AMD-Dateien `methods.js` und `planningmode.js` (+ Builds) wurden auf Nutzer-Wunsch gelöscht (Version 2026071510; nachweislich von nichts geladen, alle betroffenen Seiten laden fehlerfrei). Damit ist der Aufräumpunkt komplett abgeschlossen.

- 13. Juli 2026 (bj): **D61 und D62 umgesetzt** (Claude Code, Version 2026071508). **D61:** aufklappbarer „Seminarziele"-Bereich oben in der Sequenz (Ziele als Freitext anlegen/bearbeiten/löschen, je Ziel eine Verknüpfungs-Checkliste der platzierten Seminareinheiten); Speicherung als `seminarziele`-Liste im `statejson` des Plans (kein DB-Schema-Eingriff); neue neunte Regel „Zielabdeckung" in den Didaktischen Empfehlungen (💡 wenn ein Ziel mit keiner Einheit verknüpft ist, still ohne Ziele). **D62:** geführter Lernziel-Editor als modaler Baukasten (Phase → Verb → Inhalt → Satz-Vorschau), aufrufbar am Lernziele-Feld des Einheiten-Modals (D21) und an den Seminarzielen (D61); die fünf Seminarphasen dienen als Stufen (Nutzer-Entscheidung: Phasen statt der klassischen Bloom-Stufen – Erfahrungserhebung eingeschlossen), die gewählte Phase wird im Einheiten-Modal als Seminarphase vorbelegt (D41). Präzisierung gegenüber D62 Punkt 2: Der Satz lautet grammatisch korrekt „Die Teilnehmenden können [Inhalt] [Verb]" (deutscher Infinitiv am Satzende), nicht „[Verb] [Inhalt]". Beides auf Staging (moodle501umbau) browser-verifiziert. Damit ist der gesamte Umsetzungs-Backlog abgearbeitet (offen nur noch technische Aufräumpunkte).

- 13. Juli 2026 (bi): Die unter (bh) beschriebene Verfeinerung als **formale Entscheidung D63** ins Dokument aufgenommen (Abschnitt 3). D-Range in Abschnitt 9 auf D1–D63 aktualisiert.

- 13. Juli 2026 (bh): **Verfeinerung der Vorschlagsmechanik (D14/D6)** auf Nutzer-Befund (Claude Code, Version 2026071505): Die Einheiten, die laut Themenplan zu einem reservierten Baustein gehören („GEPLANT, NOCH NICHT PLATZIERT"), waren bisher reine Info-Zeilen und verschwanden zudem komplett, sobald man EINE Einheit platzierte (der „teilweise gefüllt"-Zweig zeigte sie nicht mehr). Sie tauchten auch nicht in den generischen Bibliotheks-Vorschlägen auf (die matchen per Stichwort/Bloom, nicht die Baustein-eigenen Einheiten). Jetzt: jede noch nicht platzierte geplante Einheit bekommt einen **„＋ Platzieren"-Button** (setzt genau diese Einheit in die Reservierung, verkleinert sie um ihre Dauer – wie beim Vorschlag-Übernehmen); die Liste **bleibt nach dem Platzieren sichtbar** und zeigt die übrigen offenen Einheiten (bis die Reservierung aufgebraucht ist); die generischen Bibliotheks-Vorschläge bleiben darunter (Nutzer-Wahl: geplant zuerst, Vorschläge darunter; kein Sammel-„Alle platzieren"-Button). Zusätzlich (Nutzer-Wunsch): die Baustein-eigenen geplanten Einheiten werden aus den generischen Bibliotheks-Vorschlägen **ausgeschlossen** (`renderSuggestions` gibt die Planungs-Refs als `excluderefs` an `suggestFor`), damit dieselbe Einheit nicht doppelt (oben als „＋ Platzieren", unten als Vorschlag) erscheint. Kandidat für eine formale eigene Entscheidung (D63), falls gewünscht. Auf Staging browser-verifiziert (90→80 Min. Reservierung nach Platzieren einer 10-Min.-Einheit, restliche geplante Einheiten sichtbar geblieben).

- 13. Juli 2026 (bg): **D53 umgesetzt** (Claude Code, Version 2026071504): achte Regel „Zeitrahmen-Hinweis" in den Didaktischen Empfehlungen (nach dem Pausenhinweis). Kapazität = Summe aller Vormittag-/Nachmittag-Anker-Fenster über alle Tage (D45-An-/Abreise-Anker zählen 0), verplant = Summe aller Platzierungen (Einheiten + Pausen), analog zum Anker-Budget-Balken. Drei Fälle im etablierten Ton: überfüllt → 💡 „etwas mehr geplant, als der Zeitrahmen hergibt", deutlich zu leer (>25 % und >1 Std. frei) → 💡 „noch reichlich Platz", sonst ✓ „passt gut in den Zeitrahmen" (jeweils mit „verplant/Rahmen" in Std./Min.). Still bei fehlenden Anker-Zeiten oder leerem Plan. Info-Text des Empfehlungs-Toggles um „Zeitrahmen" ergänzt. Auf Staging (moodle501umbau, cmid 83) browser-verifiziert. Damit ist im Umsetzungs-Backlog nur noch D61+D62 offen (plus Aufräumpunkte).

- 13. Juli 2026 (bf): **D52 umgesetzt** (Claude Code, Version 2026071503): dritter PDF-Button „Materialliste-PDF erstellen" im Import/Export-Tab neben ZIM/Konzeptsammlung — je Seminartag eine über alle Einheiten **deduplizierte** Materialliste im Abgabelisten-Layout (Kästchen zum Abhaken), leerer Plan bekommt eine Hinweisseite. Dazu **dauerhaftes Logo-Setting je Aktivität**: Upload (Bild) + Positions-Auswahl (oben rechts/links) im Aktivitäts-Bearbeiten-Formular (neue Spalte `logoposition`, Datei in Filearea `logo`); das Logo fließt automatisch in den Seitenkopf **aller** PDF-Exporte (ZIM, Konzeptsammlung, Materialliste). Auf Staging (moodle501umbau) browser-verifiziert (Materialliste mit Echtdaten inkl. Dedup, Logo als Bild in allen drei Exporten, Fallback bei leerem Plan). Damit ist Thema C (D51/D54/D58/D52) vollständig umgesetzt; offener Umsetzungs-Backlog nur noch D53, D61, D62.

- 13. Juli 2026 (be): Gruppengröße-Feld (D26/D40) von der im Code vorhandenen 7-Werte-Skala (1 / 2-3 / 3–5 / 6–12 / 13–24 / 25+ / beliebig) auf **drei Cluster** umgestellt: **Gruppenarbeit (2-5)**, **Plenum (10-20)**, **beliebig**. Gilt für Editor (Bibliothek + Sequenz-Modal), beide Filter (Bibliothek + Grid), Bulk-Bearbeitung und Import-Validierung. Bestandsdaten werden beim Upgrade umgerechnet (Nutzer-Entscheidung: 2–5 → Gruppenarbeit, 6+ → Plenum, Einzelarbeit/unbekannt → beliebig) — in mod (Method-Cards-JSON) und local (`local_kgen_method`). Reine Umsetzung/Vereinfachung, kein Widerspruch zu D40 (Feld bleibt bestehen, nur die Skala wird zu Clustern). Auf Staging verifiziert.

- 13. Juli 2026 (bd): Umsetzung von Thema C „Einreichen/Austausch abschließen" gestartet (Claude Code). **D51 umgesetzt und dabei präzisiert:** Die Weggabelung „Was möchtest du einreichen?" bekommt **drei** statt zwei Optionen – die dritte („Ein Seminarkonzept einreichen") deckt den seit D51 hinzugekommenen D32-Block ab, der sonst als dauerhaft sichtbarer vierter Kasten die von D51 bekämpfte Verwirrung reproduziert hätte (vom Auftraggeber am 13. Juli bestätigt). D51 Punkt 1 entsprechend angepasst. Die beiden „bestehende Sammlung"-Blöcke sind zu einem durchgängigen Ablauf (1. Sammlung wählen → 2. Kandidaten prüfen → 3. Update-Hinweis → einreichen) verschmolzen. **D58 umgesetzt:** öffentliche Konzeptverantwortlichen-Liste im Einreichen-Tab (nur Name, kein Kontaktweg), neue Opt-in-User-Preference `mod_seminarplaner_konzeptverantwortliche_public` (Default aus), Opt-in-Schalter nur für Nutzer:innen mit `local/seminarplaner:reviewset`-Capability sichtbar. Beide auf Staging (moodle501umbau) browser-verifiziert. Backlog-Punkte D51/D58 damit erledigt. Verbleibend in Thema C: D54 (Auto-Update-Hinweis) und D52 (PDF-Materialliste + Logo).

---

## Umsetzungsstände zu einzelnen Entscheidungen

Diese Notizen hingen in der aufgelösten Repo-Kopie an den jeweiligen Entscheidungen (D53, D61,
D62). Sie sind hier erhalten, weil sie den Bauzustand beschreiben, nicht die Entscheidung.

**Umsetzungsstand (13. Juli 2026, Version 2026071504):** Umgesetzt als achte Regel in `sequenz.js` (`dramaFindings`, nach dem Pausenhinweis). Kapazität = Summe der Anker-Fenster über alle Tage (`dayFrame`/`anchorBudget`, D45-Flags automatisch berücksichtigt), verplant = Summe aller Platzierungen (`usedMinutes`, Einheiten + Pausen). Drei Fälle: überfüllt (verplant > Rahmen + 10 Min.) / deutlich zu leer (Rest > 60 Min. und verplant < 75 % des Rahmens) / passt. Still bei fehlender Anker-Zeit oder leerem Plan. Browser-verifiziert.

**Umsetzungsstand (13. Juli 2026, Version 2026071508):** Umgesetzt. Aufklappbarer „Seminarziele"-Bereich oben in der Sequenz (`#sq-goals`); je Ziel Freitext + Verknüpfungs-Checkliste der platzierten Einheiten. Speicherung als `seminarziele`-Liste im `statejson` des Plans (kein DB-Schema-Eingriff nötig – kein install.xml/upgrade). Neunte Regel „Zielabdeckung" in den Didaktischen Empfehlungen (💡 bei unverknüpftem Ziel; ✓ wenn alle verknüpft; still ohne Ziele). Browser-verifiziert (inkl. Persistenz).

**Umsetzungsstand (13. Juli 2026, Version 2026071508):** Umgesetzt als modaler Baukasten (Phase → Verb → Inhalt → Live-Satz-Vorschau → Übernehmen). **Präzisierung zu Punkt 2 (Nutzer-Entscheidung 13. Juli):** Statt der klassischen sechs Bloom-Stufen dienen die **fünf Seminarphasen** als Stufen (Orientierung, Erfahrungserhebung, Analyse, Handlungsteil, Transfer) – eine Taxonomie, konsistent mit dem Rest des Plugins, die Phase ist zugleich die Vorbelegung (Punkt 3). **Präzisierung zum Satz:** grammatisch korrekt „Die Teilnehmenden können [Inhalt] [Verb]" (deutscher Infinitiv am Satzende), nicht die im ursprünglichen Wortlaut notierte englische Reihenfolge „[Verb] [Inhalt]". Einbindung an beiden Stellen: Button „✎ Lernziel formulieren" am Lernziele-Feld des Einheiten-Modals (fügt den Satz an, belegt die Seminarphase vor) und Button „✎ Formulieren" an den Seminarzielen (legt daraus ein Ziel an). Browser-verifiziert an beiden Stellen.
