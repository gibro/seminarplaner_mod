# mod_konzeptgenerator

Initial scaffold for the new course activity plugin.

## Next implementation steps
1. Expand `db/install.xml` based on `/docs/implementation/03_data_model.md`.
2. Implement module entry points (`lib.php`, `view.php`, `mod_form.php`).
3. Add grid services and soft-lock service in `classes/local/service/`.
4. Add activity import/export controllers for legacy `mod_data` compatibility.
